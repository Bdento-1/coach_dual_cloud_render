# 📄 .github/workflows/voice.yml
name: Voice TH
on:
  repository_dispatch:
    types: [voice_speak_th]

jobs:
  speak:
    runs-on: ubuntu-latest
    steps:
      - name: Show message
        run: echo "Message: ${{ github.event.client_payload.message }}"
